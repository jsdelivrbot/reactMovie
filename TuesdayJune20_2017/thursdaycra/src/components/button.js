import React from 'react'

const styles = ''

const Button = props => {
  return (
    <div className="">
      <button className="Bold Pink">{props.children}</button>
    </div>
  )
}

export default Button
